<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
     <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <ul class="navbar-nav">
            <li class="nav-item">
            <%= link_to "Home","/" , class:"nav-link" %>
            </li>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
               <% if Current.user || Current.owner  %>
                      <li class="nav-item">
                          <% role = Current.user.present? ? Current.user : Current.owner %>
                          <span class="navbar-text "> <%= role.first_name %> </span>
                      </li>
                          <li class="nav-item">
                        <%= button_to "Logout", logout_path, class:"btn btn-primary", method:"delete" %>
                      </li>
                  </ul>
              <% else %>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <%= link_to "Sign-Up", "/users/new", class:"nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Log-In", "/users/sign_in", class:"nav-link" %>
                </li>
            </ul>
            <% end %>
      </div>
  </div>
</nav>
